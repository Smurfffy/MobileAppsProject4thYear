<UserControl
    x:Class="HabitHero.AppPages.HabitForm"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HabitHero.AppPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Grid.RowDefinitions>
            <!--Defining rows for the app space I want-->
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Vertical">
            <TextBlock Text="Your Habit:"/>
            <TextBox x:Name="habitTxtBox"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Vertical">
            <TextBlock Text="Habit Info:"/>
            <TextBox x:Name="infoTxtBox" Height="150"/>
        </StackPanel>

        <Button x:Name="addHabitBtn" Grid.Row ="2" Content="Save" HorizontalAlignment="Left" Click="addHabitBtn_Click"/>
        <Button x:Name="exitFormBtn" Grid.Row ="2" Content="Exit" HorizontalAlignment="Right" Click="exitFormBtn_Click"/>
    </Grid>
</UserControl>
